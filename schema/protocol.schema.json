{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sherifhanna700.github.io/canotto-lab/schema/protocol.schema.json",
  "title": "Canotto protocol",
  "description": "The timings and temperatures of a biga canotto dough. Every duration is a parameter, and the 19-step protocol is solved backwards from the moment the first pizza goes on the deck. Temperatures are always degrees Celsius, whatever the app is displaying.",
  "type": "object",
  "properties": {
    "bigaMixMin": { "type": "number", "minimum": 0, "description": "Dissolving the yeast and the coarse cluster mix, in minutes." },
    "bigaRestHours": { "type": "number", "minimum": 0, "description": "Ambient rest before the biga goes cold." },
    "bigaRoomTempC": { "type": "number", "description": "Temperature of that ambient rest." },
    "bigaColdHours": { "type": "number", "minimum": 0, "description": "Cold hold for the biga before the final mix." },
    "bigaFridgeTempC": { "type": "number", "description": "Temperature of the biga cold hold." },
    "finalMixMin": { "type": "number", "minimum": 0, "description": "Final mix, from tearing the biga to the tub." },
    "benchRest1Min": { "type": "number", "minimum": 0, "description": "Rest before the first coil fold." },
    "benchRest2Min": { "type": "number", "minimum": 0, "description": "Rest before the second coil fold." },
    "oilRestMin": { "type": "number", "minimum": 0, "description": "Rest under the oil veil before balling." },
    "ballingMin": { "type": "number", "minimum": 0, "description": "Time taken to divide and ball." },
    "benchTempC": { "type": "number", "description": "Temperature through the mix and bench work." },
    "coldProofHours": { "type": "number", "minimum": 0, "description": "The balls' cold proof. Usually the longest phase." },
    "fridgeTempC": { "type": "number", "description": "Cold proof temperature. The single biggest lever on timing." },
    "temperHours": { "type": "number", "minimum": 0, "description": "Counter temper before the bake." },
    "roomTempC": { "type": "number", "description": "Temperature of the counter temper." },
    "preheatMin": { "type": "number", "minimum": 0, "description": "Oven heat soak." },
    "stretchMin": { "type": "number", "minimum": 0, "description": "Opening the ball, before launch." },
    "bakeSec": { "type": "number", "minimum": 0, "description": "Bake time." },
    "deckTempC": { "type": "number", "description": "Target floor temperature." },
    "domeTempC": { "type": "number", "description": "Target dome temperature." },
    "ddtC": { "type": "number", "description": "Desired dough temperature at the end of the final mix." },
    "freezeFlashMin": { "type": "number", "minimum": 0, "description": "Uncovered flash freeze, for balls going to the freezer." },
    "thawFridgeHours": { "type": "number", "minimum": 0, "description": "Slow fridge defrost for frozen balls." },
    "frozenTemperHours": { "type": "number", "minimum": 0, "description": "Counter temper for thawed balls, longer than for fresh." }
  },
  "required": ["bigaRestHours", "bigaColdHours", "coldProofHours", "temperHours", "fridgeTempC", "roomTempC", "bakeSec"],
  "additionalProperties": true
}
