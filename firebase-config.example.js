// Optional. Copy to firebase-config.js, fill in your project's web config, and
// add a <script src="firebase-config.js"></script> before the module script in
// index.html. Skip this entirely and the app will ask for the config in
// Help & data instead, storing it in the browser.
//
// firebase-config.js is gitignored so your keys stay out of the repository.
window.FIREBASE_CONFIG = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
